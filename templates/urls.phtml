<?= $this->fetch('_header.phtml', ['activeNavlink' => $activeNavlink]) ?>
<main class="flex-grow-1">
  <div class="container-lg mt-3">
    <h1>Сайты</h1>
    <div class="table-responsive">
      <table class="table table-bordered  table-hover text-nowrap" data-test="urls">
        <thead>
          <tr>
            <th>ID</th>
            <th>Имя</th>
            <th>Последняя проверка</th>
            <th>Код ответа</th>
          </tr>
        </thead>
        <tbody>
          <?php foreach ($sites as $site): ?>
            <tr>
              <td class="text-break"><?= htmlspecialchars($site->getId()) ?></td>
              <td class="text-break"><a href="/urls/<?= htmlspecialchars($site->getId()) ?>"><?= htmlspecialchars($site->getUrl()) ?></a></td>
              <td class="text-break"><?= htmlspecialchars($site->getTimestamp()) ?></td>
              <td class="text-break"><?= htmlspecialchars($site->getLastStatusCode()) ?></td>
            </tr>
          <?php endforeach; ?>
        </tbody>
      </table>
    </div>
  </div>
</main>
<?= $this->fetch('_footer.phtml') ?>